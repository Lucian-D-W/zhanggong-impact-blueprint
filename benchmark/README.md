# Stage9 Benchmarks

These lightweight repositories exist to validate daily-driver behavior:

- `tsjs_medium/` for medium-scale TS/JS flows
- `react_sql/` for React + SQL/PostgreSQL flows
- `generated_noise/` for generated/cache noise handling
- `ambiguous_multifile/` for multi-file seed ranking and trust decisions
