# tsjs_pg_compound

Stage5 mixed-repo fixture for:

- primary adapter: `tsjs`
- supplemental adapter: `sql_postgres`

This fixture validates that one repo can merge TS/JS application code and
PostgreSQL routines into the same graph and report.
