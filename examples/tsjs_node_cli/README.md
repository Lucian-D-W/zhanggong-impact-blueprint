# TS/JS Node CLI Fixture

This fixture exercises the stage4 `node-cli` TS/JS profile.

It is intentionally small but real enough to verify:

- profile-aware init / detect / doctor
- `.js` plus `.ts` family scanning
- exported const arrow functions
- direct `CALLS` and `IMPORTS`
- node:test execution
- raw V8 coverage import
