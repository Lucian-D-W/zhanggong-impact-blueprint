# React Vite Profile Fixture

This fixture exercises the stage4 `react-vite` profile without turning the
skill into a platform.

It exists to validate:

- `.tsx` and `.jsx` discovery
- React function component detection
- custom hook detection
- class method detection
- profile-aware detect / doctor / build / report / after-edit
- raw V8 coverage through a lightweight Node test command
